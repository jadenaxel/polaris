/** @format */

export default `POLARIS HELP

new: Can create a project.
     Ex: polaris new 'name'

make: Can create a everything you need.
      nav: Without any subcommnad, create the stack navigation.
      - Ex: polaris make:nav

      nav: With a commnad material, create the stack navigation with the bottom material navigation.
      - Ex: polaris make:nav material

      nav: With a command bottom, create the stack navigation with the bottom navigation.
      - Ex: polaris make:nav bottom`;
